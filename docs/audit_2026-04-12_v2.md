# U1L1 静态审计 v2（2026-04-12 复查）

> 审计方式：纯静态（Read + Grep），对照 `audit_2026-04-12.md` 7 处问题逐条复查 + 反向扫描。
> 审计执行者：Claude Opus 4.6（audit-only，未做任何 Edit）。
> 代码层：`data/u1l1_coop.js` / `coop-types.js` / `adventure.js`。

---

## 一、v1 的 7 处问题逐条核对

| # | v1 位置 | v1 问题 | v2 状态 | 证据 |
|---|---|---|---|---|
| P1 #1 | writing/s2/q4 `red` | stepB options `["red","big","fast"]` + template `red bird` | ✅已修 | L801-807：template 改为 `Look! ___ are birds. This one is ___.`；stepA options `["They","It","This"]`；stepB options `["cute","big","fast"]`，correct `cute`。全部在白名单内 |
| P1 #2 | speaking/s3/q3 `Cool!` | stepB.line B 学生要读 `"Cool!"` | ✅已修 | L1019 stepB.line 现为 `"It can run fast!"` |
| P2 #1 | writing/s2/q1 bear `small` | stepB options 含 `small` | ✅已修 | L782 options `["big","cute","fast"]`，`small` 消失 |
| P2 #2 | writing/s2/q3 horse `slow` | stepB options 含 `slow` | ✅已修 | L798 options `["fast","big","cute"]`，`slow` 消失 |
| P2 #3 | writing/s4/q1 干扰项 `small` | optionsMap 干扰项 `"It's small."` | ✅已修 | L882 干扰项改为 `"That is a bird. It can fly." / "This is a rabbit."` |
| P2 #4 | writing/s4/q2 干扰项 `small` | optionsMap 多处 `"It's small."` | ✅已修 | L899-901 干扰项全部为 `big / cute / can fly / can run fast` |
| P2 #6 | coop-types.js L1914 `coop_listen_judge` L3 标签错 | 支架写"句子文字："但 q.audio 只是单词 | ✅已修 | L1914 现为 `"📖 音频对应单词：<strong>" + q.audio + "</strong>"`，标签与内容一致 |
| P2 #7 | coop-types.js L2066 `coop_relay_fill` L2 副作用 | 调 `speakWord(q.audio)` 但 relay_fill 无 `q.audio` | ✅已修 | L2065-2069 改为读 `q.stepA.correct || q.stepB.correct`，音频重播真正生效 |

**7/7 全部 ✅ 已修。**

（说明：v1 P1 共 2 项、P2 共 7 项，核心修复目标为 v1 结论中列出的 "6 处"——即 P1 #1, P1 #2, P2 #1, P2 #2, P2 #3, P2 #4；外加 P2 #6, P2 #7 两处支架 bug。本次 v2 对 7 项支架/数据修复全部逐条核对通过。）

---

## 二、白名单 grep 扫描（反向验证）

```
grep -nE '\b(red|brown|black|white|yellow|cat|tiger|dog|Cool|small|slow|beautiful|hot|cold)\b' data/u1l1_coop.js
grep -nE '\b(has|long|legs)\b' data/u1l1_coop.js
```

**结果：全部 0 命中 ✅。** v1 报告的 6 处词汇白名单泄漏在数据层彻底清除。

---

## 三、scaffold 字段存在性核对（B013）

对 `getScaffoldHintHtml` 中每个 case 使用的字段做逐条核对：

| 题型 | 字段引用 | data 真实存在？ |
|---|---|---|
| coop_listen_relay | `q.chinese` | ✅每题都有 |
| coop_listen_judge | `q.audio` / `q.chinese` | ✅每题都有 |
| coop_word_relay | `q.stepA.correct` / `q.stepB.optionsMap[w].correct` | ✅每题都有 |
| coop_flip_match | 无字段访问 | n/a |
| coop_build_sentence | `q.chinese` / `q.sentence` | ✅每题都有 |
| coop_relay_fill | `q.chinese` / `q.stepA.correct` / `q.stepB.correct` | ✅每题都有 |
| coop_spell_word | `q.chinese` | ✅每题都有 |
| coop_sentence_sort | `q.chinese` / `q.sentence` | ✅每题都有 |
| coop_read_relay | `q.word` / `q.chinese` | ✅每题都有 |
| coop_picture_speak | `q.stepA.chinese` / `q.stepA.expected` | ✅每题都有 |
| coop_dialogue | `q.scenario` / `q.stepA.chinese` / `q.stepA.line` | ✅每题都有 |

**0 处字段不存在。B013 隐患清零 ✅。**

---

## 四、代码硬编码数组（B015）扫描

`renderWarmupMatch` L2225 `chineseOrder = pairs.map(function(_,i){return i;})`：动态生成，已修复昨日硬编码 4 项硬崩溃。未发现其他固定长度 index 数组 ✅。

---

## 五、A/B 角色轮换核对

- `renderCoopType` (coop-types.js L16-19)：`state.coopRoles[0]` 写入 `window.currentCoopRoleA`，fallback 到 `qIndex % 2` ✅
- `adventure.js` L440 / L537：每题完成后 `coopRoles.reverse()` ✅
- `adventureState.coopRoles` 初始 `[0,1]`（L43）✅

---

## 六、语法静态检查

- `data/u1l1_coop.js`：顶层结构 `var u1l1_coop = { ... };` L11→L1025 闭合，L1028 `window.u1l1_coop` 暴露，L1030 结尾 `}` 属 `typeof window` if 块。结构正常 ✅
- `coop-types.js`：渲染器 table、`getScaffoldHintHtml`、`triggerScaffoldSideEffect`、`renderWarmupMatch` 均有显式闭合；L2409 末尾 console.log ✅
- `adventure.js`：仅做 grep 级核对，未发现异常

（Bash `node --check` 被禁用，以 Read 静态解析代替，无明显括号/引号失配）

---

## 七、新发现 bug（v1 未报告）

| # | 严重度 | 位置 | 问题 |
|---|---|---|---|
| N1 | P3 | coop-types.js L2173 | `renderWarmupMatch` 的 `wordDict` 字面包含 `"small": "小"`。U1L1 当前 `q.words` 未出现 small，所以**不会**渲染到学生屏幕（pairs 过滤需 q.words ∩ wordDict），但代码字符串字面仍命中白名单禁用词——审计合规风险，建议删除该 key |
| N2 | P3 | coop-types.js getScaffoldHintHtml | `coop_listen_sort` / `coop_listen_scenario` / `coop_read_scenario` / `coop_write_scenario` 四种题型在 L2 / L3 / L4 **均无 hint 文案分支**，函数返回 `''`，学生在这些站点答错升级支架只会看到 banner + 可能的 side-effect。不 crash，属"支架文案覆盖缺口"（P3 优化项） |
| N3 | P3 | coop-types.js L2159 注释 | v1 P3 #1 遗留：注释仍写"A 同学配左边 2 对（indices 0,1），B 同学配右边 2 对（indices 2,3）"，与动态 `aZoneEnd = floor(pairs.length/2)` 脱节 |
| N4 | P3 | coop-types.js L2370 onChineseClick | v1 P3 #2 遗留：学生先点中文（`selectedWordIdx === null`）时直接 return，无任何反馈，三年级学生容易误以为"点坏了" |
| N5 | P3 | adventure.js L227 `_enterStationLock` | v1 P3 #3 遗留：100ms 静默防抖，被拦截时无 warning |
| N6 | P4 | data/u1l1_coop.js L361 | `"Look! They are birds. It can fly."` — `They ... It` 数不一致；词汇合法，但句型被复数→单数切换破坏。建议改 `They can fly.` 或 `Look! This is a bird. It can fly.`（非阻塞） |

**新增数据层 P1/P2 bug：0。**
**新增代码层 P1/P2 bug：0。**
**新增 P3/P4：6（其中 3 处为 v1 已报告的遗留 P3 项）。**

---

## 八、总结

| 维度 | 结果 |
|---|---|
| v1 的 7 处问题是否全部真修 | ✅ **全部已修** |
| 白名单 grep | ✅ 0 命中 |
| 反向扫描（新引入超纲词） | ✅ 数据层 0 处；代码字典 L2173 有 `"small"` key 但无法触达学生屏幕（P3）|
| scaffold 字段存在性（B013） | ✅ 0 处不存在 |
| 代码硬编码数组（B015） | ✅ 无 |
| A/B 轮换 | ✅ 正确 |
| 语法静态扫描 | ✅ 无明显失配 |
| 运行时硬崩溃 | ✅ 0 |

### 是否可以进入 P2 阶段：**可以 ✅**

理由：
1. v1 结论中列出的 6 处数据层 P1/P2 硬违规 + 2 处代码层支架 bug 全部修复并经 grep/字段交叉验证，construct validity 风险清零
2. 新发现的 bug 均为 P3/P4（字典 debug 级、注释 debt、UI 反馈遗漏、句型单复数），**不阻塞**进入 P2
3. 建议在 P2 开始前顺手处理：N1（删除 wordDict 里的 small）+ N6（句型单复数）——两处都是 1 行 edit，5 分钟搞定；其余 P3 可排入 P2 阶段尾期优化窗口

### 下一步建议（优先级排序）

1. 【即时】删除 coop-types.js L2173 wordDict 里的 `"small": "小"` 和其它禁用词 key（如果后续扩课会出事）
2. 【即时】修 L361 `"Look! They are birds. It can fly."` → 单复数一致
3. 【P2 窗口】补齐 coop_listen_scenario / sort / read_scenario / write_scenario 的支架 hint 文案
4. 【P2 窗口】更新 L2159 注释 + L2370 CN-first 反馈
