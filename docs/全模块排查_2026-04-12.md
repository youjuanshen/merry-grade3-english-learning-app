# 全模块排查 2026-04-12（诊断，只读）

范围：B018（L3/L4 直接给中文=偷任务）+ B019（500ms auto-commit + lockOptions 锁死）+ 支架缺失 + 其他。

## 一、B019 同类问题（createOptionGrid 500ms 自动提交）

`createOptionGrid` 已支持 `confirmMode:true`（显式确认按钮）。只有 **listen_relay** 接入。

| 题型 | 渲染函数 | createOptionGrid 行 | confirmMode | 严重度 |
|---|---|---|---|---|
| coop_listen_relay | renderCoopListenRelay / renderStepB_ListenRelay | 246, 276 | ✅ | — |
| coop_listen_judge（B纠错） | renderStepB_ListenJudge | 367 | ❌ 旧 500ms | P1 |
| coop_listen_scenario | renderCoopScenario / renderStepB_Scenario | 702, 752 | ❌ 旧 500ms | P1 |
| coop_word_relay | renderCoopWordRelay / renderStepB_WordRelay | 768, 793 | ❌ 旧 500ms | P1 |
| coop_relay_fill | renderCoopRelayFill / renderStepB_RelayFill | 1352, 1375 | ❌ 旧 500ms | P1 |

共 **4 个题型 / 8 处调用未修**。表现：点图 500ms 后 lockOptions → pointerEvents:none 全锁死，学生无法改主意。`listen_judge` StepA 的 Y/N 按钮用 `lockOptions(btnGrid)` 立即锁（307/324），但因只有 2 选项且 A 的行为是"判对错"，即时锁死尚可讨论，P2。

其他题型不走 createOptionGrid：flip_match（翻牌）、sentence_sort（拖词块）、build_sentence（拖词块，300ms auto 完成最后一块）、spell_word（拖字母）、read_relay / picture_speak / dialogue（口语自评）—— **提交都是显式**（放完最后一块 / 自评按钮），且词/字母块支持回撤（attachSlotClickA）。未发现新的隐式 500ms auto-commit 模式。

## 二、B018 同类问题（L3/L4 支架直接给中文翻译）

依据 `docs/scaffold_audit_2026-04-11.md`，listen_relay 已在 getScaffoldHintHtml:1950 重设计。剩余未修：

| 题型 | L3 违规 | L4 违规 | 行 |
|---|---|---|---|
| coop_listen_judge | 📖 直接显示英文原文（把"听"变"读"） | 文+音+中文翻译 | 1965 | P1 |
| coop_word_relay | 📖 直接给中文 `wrZh` | 字母+中文+词 | 1978 | P1 |
| coop_read_relay | 📖 中文意思 | 英+中+音频 | 2041 | P1 |
| coop_relay_fill | 💡 空格中文 | 图+中+音 | 2019 | P1 |
| coop_build_sentence | （L2 已给中文，L3 OK，L4 全词块编号=删任务） | — | 2008 | P2 |
| coop_sentence_sort | — | L4 给中文翻译 | 2033 | P2 |
| coop_flip_match | — | L4 直接显示配对位置=删任务 | 2001 | P2 |
| coop_spell_word | — | L4 给中文翻译 | 2026 | P2 |
| coop_picture_speak | L2 给中文=绕过看图说 | L4 给完整参考词 | 2049 | P2 |
| coop_dialogue | L2 给中文 | L4 给完整原句 | 2064 | P2 |

P1 共 **4 个**（听力/读音直接"偷任务"），P2 共 **6 个**（阅读/写作/口语类，偏"拐杖"而非删任务，争议较小）。

## 三、支架缺失 / 渲染异常

- `getScaffoldHintHtml` 对 **coop_listen_sort**、**coop_listen_scenario** 无 case 分支 → 答错只显示 Level 1 banner，L2/L3/L4 返回空串。P1。
- `coop_category_sort`（若存在，见 bug-patterns B011 提及）在 getScaffoldHintHtml 无 case → P1（待确认题型是否还在使用）。
- 支架渲染位置：`showCoopRetryPrompt` 里的 hintHtml（adventure.js:622）只为 listen_judge / stepB.hint 两种情况拼，其余依赖重渲染+`showScaffoldForLevel`；若题型有大图区域，4 级支架横幅插入位置在 container 顶部，可能挤压图片；需实机确认。P2。

## 四、汇总

- **P1 共 9 处**：B019 未修 4 题型 + B018 听力/读音类 4 题型 + 支架分支缺失 2 题型（有重叠：listen_judge / word_relay / relay_fill 同时中两类 bug）
- **P2 共 6 处**：B018 阅读/写作/口语类 scaffold "拐杖"问题
- **无新增 bug 模式**

推广方案已准备好（createOptionGrid confirmMode 无侵入，只改调用点）；B018 需要逐题型设计新 hint（工作量较大，需先确认听力类 4 个）。
